# Risk Register

Moved out of `docs/MIGRATION_PROGRESS.md` (Phase 5) so risk tracking doesn't
live inside a changelog. Update in place as risks are mitigated or new ones
surface; don't delete resolved rows, mark them Resolved with how.

| # | Risk | Impact | Status | Mitigation |
|---|---|---|---|---|
| 1 | Customer/order PII committed to a public repo | High | Mitigated | `.gitignore` covers `dump.sql`, `migration/data/*.json`, and the generated import CSVs; verify `git status` before every commit |
| 2 | Shopify CSV import silently drops/misreads a field because Shopify's format changed since implementation | Medium | Open | Re-verify column headers against `help.shopify.com` before each real import, not from memory |
| 3 | SEO loss from URL structure change (WooCommerce slugs vs Shopify) | High | Mitigated (Phase 5) | Full redirect matrix built and validated (`reports/redirect_matrix.csv`, 875 URLs); remaining exposure is the tag→`/collections/all` fallback (ADR-003) and the brand/category slug collision (see below) |
| 4 | Order history isn't importable via product/customer CSV | Medium | Open | Needs a dedicated decision in Phase 11 (Matrixify vs. Transporter vs. Admin API vs. accept the gap) |
| 5 | Category "VALENTINE COMBO DEALS" and brand "VALENTINE COMBO DEALS" share a slug, both mapping to `/collections/valentine-combo-deals` | Medium | Resolved (Phase 6) | Root cause found: it's a mislabeled seasonal bundle promo, not a real brand (8 products, all gift sets). Reclassified as a manual collection and dropped from the brand/Vendor system — ADR-007. Same fix applied to "TRADEFAIR COMBO DEALS" pre-emptively. |
| 6 | Site navigation is partly theme/page-builder-driven (footer, WooCommerce account UI), not fully represented in `wp_nav_menu` data | Medium | Mitigated (Phase 6 design) | Shopify footer will be a real navigation menu (`docs/SHOPIFY_ARCHITECTURE.md` § Navigation), not theme-hardcoded — fixes the blind spot instead of reproducing it. Still needs Phase 7 build-out. |
| 7 | "Contact" and "Cookie Policy" pages have no discoverable inbound link (menu, content, or footer) on the live site | Low | Open | Included in the planned Shopify footer menu regardless (`docs/SHOPIFY_ARCHITECTURE.md`); their absence today is treated as a defect being fixed, not preserved |
| 8 | Live Cookie Policy page has an incorrect meta description (copy-pasted from Terms & Conditions) — pre-existing WooCommerce-side bug, not caused by migration | Low | Open | Fix when porting page copy to Shopify (Phase 6/7) rather than carrying the error forward |
| 9 | `csv_generator.py`'s `Type` column uses the first assigned category, not the most specific one — doesn't match the Product Type strategy in `docs/SHOPIFY_ARCHITECTURE.md` (ADR-009) | Medium | **Resolved (Phase 9)** | `database_parser.py` now computes the most-specific category via real parent-chain depth walking (`most_specific_category_for()`); `csv_generator.py` reads it from the new `product_type` field. Verified on real data (e.g. product 17: categories `['Face','Foundation']` → `product_type='Foundation'`, not `'Face'`) |
| 10 | ~12 WooCommerce categories are miscategorized at top level (should be Level 2/3 content) and need merchant sign-off before becoming Shopify collections | Low | Resolved (Phase 6.5) | Cleanup mapping approved and applied in `docs/SHOPIFY_FOUNDATION.md` § Collection architecture |
| 11 | The 8 reclassified bundle products (ADR-007) need a manual per-product check for their real Vendor value | Low | Open | Not automated — guessing a manufacturer from a bundle listing risks being wrong. Manual review before Phase 9 |
| 12 | 37 of 166 `pwb-brand` terms have zero published products | Low | Informational | No action needed — Vendor/brand-collection creation in Phase 9 naturally skips brands with no products, since it's driven by what's actually on live products |
| 13 | Product tag casing/naming is inconsistent (e.g. "MASCARA" vs lowercase tags elsewhere) | Low | Open | Needs a normalization pass before tags are used as Search & Discovery filters (`docs/SHOPIFY_ARCHITECTURE.md` § Search & Discovery filters) |
| 14 | One `pwb-brand` term has name "Accessories _make_up" and slug `unnamed` (5 products) — clearly a data-entry error | Low | Open | Manually identify these 5 products' real brand/vendor before Phase 9 (`docs/SHOPIFY_FOUNDATION.md` § Collection architecture) |
| 15 | ~~"Topicrem" (2 products), "Tropicrem" (1), "Topicream" (1)~~ | Low | **Resolved (Phase 9), corrected count** | Real investigation: only **2 products** are involved (not 4) — each is tagged with both the correct "Topicrem" term and one misspelled duplicate simultaneously. `database_parser.py`'s `BRAND_NAME_CORRECTIONS` now normalizes deterministically; verified both products show `vendor='Topicrem'` after regeneration (`docs/PHASE9_PRODUCT_IMPORT.md`) |
| 16 | 14 product images are AVIF format, which isn't on Shopify's supported product-image format list (verified against current Shopify docs, not assumed) | Medium | Open | Convert to WebP or JPEG before Phase 8 media migration (`docs/SHOPIFY_FOUNDATION.md` § Asset inventory) |
| 17 | `code-snippets` plugin is active; its actual snippet content wasn't read from the database (out of scope for a SQL-dump-only analysis) | Medium | Mitigated (Phase 7 acceptance review) | All 24 real snippets' actual code extracted and classified KEEP/REPLACE/RETIRE/INVESTIGATE — see `docs/PHASE7_ACCEPTANCE.md` § wp_snippets findings. Remains Open at the GitHub-issue level since several REPLACE/INVESTIGATE items aren't built/decided yet, but the "unknown content" risk itself is resolved |
| 18 | Both FluentCRM and MailerLite (official-mailerlite-sign-up-forms) are active simultaneously — unclear which is the real email marketing system of record | Low | Open | Ask the client before building any email-capture integration beyond the plain newsletter form already in the theme |
| 19 | Point-of-sale plugin (`wp_apbd_pos_*` tables) isn't in the current active-plugin list, but if in-store POS is still actually used via a mechanism this check wouldn't catch (e.g. a must-use plugin), it needs Shopify POS as a real replacement | Low | Open | Confirm directly with the client before assuming "not applicable" (`docs/SHOPIFY_FOUNDATION.md` § App replacement matrix) |
| 20 | Two active plugins (`ai-provider-for-anthropic`, `ai-provider-for-google`) have an unclear purpose from database evidence alone | Low | Open | Ask the client what these power before deciding on a Shopify equivalent |
| 21 | Shopify CLI (and its bundled Theme Check) could not be installed in this environment — Node.js install was blocked by a Windows admin-elevation prompt that can't be approved non-interactively | Medium | Resolved (Phase 7 acceptance review) | Worked around using Node.js's portable (zip) distribution, which needs no elevation. `shopify theme check` now runs cleanly: 68 files, 0 offenses (after fixing 2 real errors it found in `config/settings_schema.json`). Note: `shopify theme dev` (live local preview) is a separate capability still blocked — it requires a connected live Shopify store, which doesn't exist yet; that's a real prerequisite, not a tooling gap |
| 22 | Markets, multi-currency, and B2B business requirements (which countries/currencies, whether company accounts are wanted) have never been decided by anyone | Low | Open — formalized | Was informal prose in `docs/THEME_ARCHITECTURE.md`; now a tracked decision record, `docs/DECISIONS.md` ADR-010 ("Business Decision Required"). Still genuinely undecided; theme remains code-ready without market-specific config or B2B UI |
| 23 | The variant picker's multi-option (2-3 option) code path is written generically but only tested conceptually — this catalog's real data never exceeds 1 option (Shade/Size) | Low | Open | Verify against real multi-option data if/when the catalog ever has any; not a blocker since the single-option path (100% of current products) is what's actually exercised |
| 24 | 24 specific WooCommerce product IDs were flagged by the site owner's own (now-retired) diagnostic tooling as having potential price-data integrity issues (duplicate price meta rows, product-lookup-table mismatches) before migration | Medium | **Investigated (Phase 9)** | Confirmed real: all 24 have `price` set but `regular_price`/`sale_price` empty. **Broader pattern found**: 96 of 611 products (15.7%) share this pattern, not just the 24 flagged. **Impact assessed**: doesn't break the import — Compare At Price is correctly left blank rather than fabricated. 10 products (subset, all draft) have no price at all - `docs/PHASE9_PRODUCT_IMPORT.md` § Price integrity |
| 25 | ~~Brand logo images exist in `wp_termmeta` (key `pwb_brand_image`)~~ | Low | **Corrected (Phase 8), not just resolved** | This was a Phase 7 review finding based on reading a WordPress snippet's *code*, not the *data*. Phase 8 checked the actual data: `wp_termmeta` has **zero rows of any kind** for any of the 166 `pwb-brand` terms — the key was never populated. No brand logo source exists to migrate; see `docs/MEDIA_MIGRATION.md` § 5. `brand` metaobject `logo` fields start empty; logos need sourcing fresh, not migrating |
| 26 | "Free Delivery Threshold Notice" is real, active WooCommerce functionality (checkout messaging: "£X away from free delivery") with no Shopify equivalent built yet | Low | Open | Not a Phase 7 blocker (checkout-independent, could be built into the cart drawer/page); real content/merchandising gap for a future issue, not built during the Phase 7 acceptance review per "don't alter working code without a defect" |
| 27 | At least 1 of 6 spot-checked media files has a live server filesize that doesn't match its WordPress database metadata (`IMG_0992.jpeg`: DB says 771,033 bytes, live file is 693,042 bytes) | Low | Open | Likely a file replaced/re-compressed outside the Media Library UI (FTP/hosting panel), a known WordPress behavior. DB metadata can't be assumed 100% current for every file; recommend a broader filesize-verification pass before Phase 9 treats `migration/data/media_manifest.json`'s `file_size` field as authoritative (`docs/MEDIA_MIGRATION.md` § 16) |
| 28 | Real hex color data exists for all 332 shade/color attribute terms (`wp_termmeta.rey_attribute_color`) but Phase 7's theme build used text-pill swatches instead, believing no color data existed | Low | Open — quantified (Phase 9) | Of 326 real variation-level shade/color assignments: 132 (40.5%) have a real hex color available, 184 (56.4%) reference a real term with no color set, 10 (3.1%) are orphaned references to a nonexistent term. Proposed mapping (not approved) in `docs/PHASE9_PRODUCT_IMPORT.md` § Colour/shade mapping, per issue #37 |
| 29 | Category thumbnails (`wp_termmeta.thumbnail_id` on `product_cat` terms): all 46 categories have this field present but set to `'0'` (WordPress's "not set" convention) — zero categories have a real category image assigned | Informational | N/A | Not a defect — genuinely never configured. Category collections in Shopify will need images sourced fresh during Phase 9/theme setup, same situation as brand logos (risk #25) |
| 30 | 12 draft products had a completely empty Shopify handle (`post_name` blank — normal for WordPress drafts never explicitly published) — would have silently merged into a single product on import | Medium | **Resolved (Phase 9)** | `database_parser.py`'s `unique_handle()` now falls back to a slugified title with a collision-guard suffix. Verified: 611 products, 611 distinct non-empty handles after regeneration |
| 31 | 5 products have vendor `'Accessories _make_up'` (slug `unnamed`) — a data-entry placeholder, not a real brand | Medium | Open — quarantined, not guessed | Product IDs: 1726, 2369, 2370, 2371, 2372. Per this phase's stop conditions, ambiguous business classification isn't automated — needs a human to determine each product's real vendor (`reports/phase9_product_data_quality.csv`, code `ambiguous_vendor`) |
| 32 | ~~The approved Phase 6.5 category-cleanup mapping was documented but never mechanically applied~~ | Medium | **Resolved (2026-08-10, issue #39)** | Applied mechanically as `CATEGORY_CLEANUP_MAP` in `migration/scripts/phase9_dry_run.py` (7 of the 8 stray categories — `Body Body`→Body Care, `TRADEFAIR COMBO DEAL`→Tradefair Combo Deals, `Glow serum`/`Dark spots &amp; Discoloration serums`/`Eye cream`/`Glow spray`→Serums & Treatment, `sponge`→Tools & Accessories — every target verified to exist in `shopify/foundation/collections.json` before mapping to it). **"Uncategorized" (4 products) deliberately excluded** — the approved architecture explicitly says it "needs a manual audit... not a 1:1 collection," so mapping it would not be implementing the approved plan, it would be inventing a new one. Verified: `distinct_collections_referenced_and_resolved` 151→158, `distinct_categories_or_brands_unmapped` 31→24 (the remaining 24 are expected Level-3 product-type categories per ADR-009, plus Uncategorized). Re-ran `phase9_dry_run.py` fully — product/variant counts unchanged, 0 regressions. **Follow-on fix, same session**: `seo_url_mapper.py`'s `/product-category/` redirect targets for these 7 categories previously pointed at the raw WordPress slug (e.g. `/collections/body-body`) which will never become a real collection now — would have been a 301 to a 404. Fixed to redirect to the real consolidated handle instead, reusing `CATEGORY_CLEANUP_MAP` as the single source of truth rather than a second copy of the mapping. Verified: 0 `/shop//` regressions (risk #30 stays fixed), 0 broken internal links; `duplicate_urls.csv` collision count went from 1→4, all 4 confirmed intentional many-to-one redirect consolidation (3 new, from this fix; 1 pre-existing, the already-known Valentine/Tradefair brand-vs-category case), not a real handle problem |
| 33 | ~~Product 1721 (Ezanic Azelaic Acid Gel) has a variable-product attribute with a completely blank name~~ | Low | **Resolved (post-test review, 2026-08-10)** | Root cause found in the raw `_product_attributes` serialized data: the real WooCommerce attribute name is the single character `"%"` (matches the `20%`/`10%` option values) — `database_parser.py`'s `attribute_label()` was deriving the label from the array key (WooCommerce's `sanitize_title(name)`, which strips `%` to an empty string) instead of the real `name` field, only for this one case where the derived key is empty. Fixed with a minimal fallback (`attribute_label(...) or info['name']`), verified against the other 9 custom-attribute products in the catalog to confirm zero regressions. Re-ran the test import: product 1721 now has both real, correctly-priced variants — confirmed via live reconciliation, not the importer's own report |
| 34 | Products with a blank `vendor` (ADR-007 reclassified bundle products, 20 of 611 total) get Shopify's shop name ("Wholesale Beautyhub") auto-filled as Vendor on `productCreate`, rather than staying blank — a real Shopify platform behavior discovered via the test import's live reconciliation (product 7535), not documented in any Shopify doc consulted earlier in this project | Low | Open — needs a decision | Doesn't break the import (the field isn't empty, just not what the source data specified), but means these 20 products would visibly show "Wholesale Beautyhub" as their brand unless explicitly overridden. Needs a decision before the full import: accept the shop-name default, or explicitly set a neutral placeholder value |
| 35 | ~~Shopify's `inventorySetQuantities` mutation rejects every call with "The @idempotent directive is required for this mutation but was not provided"~~ | Medium | **Resolved (2026-08-10)** | Root cause confirmed against real Shopify documentation (`shopify.dev`, fetched directly, not from memory): as of API 2026-04, `inventorySetQuantities` requires a client-supplied `@idempotent(key: $key)` directive on the mutation (a UUID string, generated fresh per call — this key protects a single request from double-application on retry, not meant to be stable across script runs). A second, separate real requirement found only by testing live: each `InventoryQuantityInput` entry also needs `changeFromQuantity` (the caller's belief of the current value) — implemented by batch-fetching current quantities via `nodes(ids: [...])` before every set call. Fixed in `migration/scripts/phase9_test_import.py`. **Verified against the live store**, not assumed: 9-scenario test suite (`migration/scripts/test_phase9_inventory.py`) — 8 real live-integration tests, 2 mocked failure-mode tests (missing/malformed response) — all pass. Full production re-run: live reconciliation went from 13 inventory mismatches to 0 (161 of 161 inventory-related comparisons match; only the unrelated vendor-auto-fill mismatch, risk #34, remains). Idempotency re-verified: re-running twice left quantities unchanged (converges, does not double or drift). Two related real bugs also found and fixed by the test suite itself: `fetch_current_available_quantities` crashed on a nonexistent inventory item (null `nodes` entry) and on a malformed/error-only API response (missing `data` key) — both now handled defensively rather than crashing |
| 36 | ~~The bulk-import pilot batch crashed with `IndexError` on product 18~~ | Medium | **Resolved (2026-08-10)** | Root cause confirmed against raw `wp_postmeta`: variations 10966 (product 18) and 19990 (product 16464) are real, `publish`-status WooCommerce records with a literal empty `attribute_pa_shade` value — genuinely broken source data, not a parsing bug. Full-catalog scan confirmed isolated to exactly these 2 of 611 products. ADR-012 (Option A): skip the broken variation only, import the parent with its remaining valid variations (`is_valid_variation`/`partition_variations` in `phase9_dry_run.py`). Every skip explicitly audited in `reports/phase9_skipped_variations.jsonl`. Verified live: both products reconcile with 0 mismatches, correct variant counts (9/10 and 22/23), no fabricated option value |
| 37 | ~~4 of product 69's 11 variants were created in Shopify with a fabricated £0.00 price~~ | High | **Resolved (2026-08-10)** | Root cause: the importer's pricing code defaulted to `'0.00'` whenever `regular_price`/`price` were both empty in the WooCommerce source — silently converting "price unknown" into "free." Confirms risk #24's original flag on product 69 was real. Full-catalog scan found the complete scope: product 69 (partial, 4/11 unpriced) + 10 wholly-unpriced draft products (25089, 25092, 25109, 25111, 25113, 25115, 25117, 25217, 25219, 25369). ADR-013: skip the 4 unpriced variants on product 69 (Option A, same precedent as #36), quarantine the 10 wholly-unpriced products entirely (`no_source_price`, `flag_missing_price()` in `phase9_dry_run.py`). The 4 already-live £0.00 variants were removed via the schema-verified `productVariantsBulkDelete` mutation, touching neither the parent product nor its 7 legitimate variants. Regression-tested (`test_phase9_pricing.py`, 13 assertions). Full-store verification: 0 fabricated £0.00 prices remain; 0 legitimate £0.00 prices exist anywhere in the source catalog |
