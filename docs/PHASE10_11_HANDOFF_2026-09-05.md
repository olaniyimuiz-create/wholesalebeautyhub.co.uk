# Phase 10/11 Handoff — 2026-09-05

Point-in-time state snapshot. Figures below were live-verified or traced to
committed artifacts on this date — re-verify anything time-sensitive (scopes,
credential validity, live counts) before acting on it later, especially
anything marked "flapping" below.

## Git

| | |
|---|---|
| Branch | `phase10/gate7-executor-freeze` |
| Local HEAD | `72f10d7` (doc correction, see below) |
| `origin/phase10/gate7-executor-freeze` | `8976f5e` — **`72f10d7` not yet pushed as of this snapshot** |
| `origin/main` | `5c546b0` — untouched |
| `stash@{0}` | Intact — `phase9-stale-snapshot-preserved-2026-08-17`, proven zero unique content, kept for audit only |

## Phase 10 — customer migration: EXECUTED, live-verified

**Derivation chain** (source → live):

```
13,043  wp_wc_customer_lookup source rows
12,096  classified IMPORT
  247   deferred — name conflicts, held for ADR-014 Gate 5 (not dropped)
11,849  approved run population (12,096 − 247)
11,844  live in Shopify with GID (5 failed/quarantined at write time)
 539    QUARANTINE total (292 missing email + 247 conflicting identity —
        this overlaps the 247 deferred above, it is a different cut of
        the same source rows, not additive)
```

**Live verification performed this session**: paged the full customer list
(48 pages), confirmed **11,844 customers, every one carrying
`custom.legacy_woo_customer_id`** — exact match to the run ledger, zero
duplicates.

**Platform constraint (verified)**: store runs New Customer Accounts
(`customerAccountsVersion: NEW_CUSTOMER_ACCOUNTS`). `CustomerInput` has no
password field — WooCommerce password migration is confirmed technically
impossible, not a risk to manage.

**Gate 7 authorization**: recorded in
`migration/schema/phase10_migration_contract.json` → `authorization` block as
owner-confirmed *retrospectively* (the run had already executed). That block
carries explicit provenance (who confirmed, when) and an `_evidence_gap` field
stating the primary signed artifact is not in this repository — link it there
if it surfaces.

**API scopes** — flapping, re-verify live before relying on this:

| Scope | Status at last check (2026-09-05 20:58) |
|---|---|
| `read_customers` | GRANTED |
| `write_customers` | GRANTED |
| `read_orders` | absent |
| `write_orders` | absent |

This exact `read_customers`/`write_customers` pair has gone
granted → revoked → granted more than once across this project's history.
Token in `.env` at last check: prefix `shpca_`, fingerprint `9653e6a04951`.

**Still open (business decisions, no Shopify access needed):**
- ADR-014 — FluentCRM marketing-consent carry-over. Default: consent omitted
  for all customers until owner/legal sign-off.
- Whether a Gate B written approval statement (import method) exists outside
  this repository — not located inside it.

**Documentation correction made this session**: `PHASE10_IMPORT_PROCEDURE.md`
previously opened with "Status: BLOCKED. Zero Shopify customer writes have
ever occurred" — true through 2026-08-17, false now. Corrected in place;
original line preserved with a note marking it historical, not current.

## Phase 11 — historical orders: DISCOVERY + ARCHITECTURE ONLY, zero mutations

| Metric | Value |
|---|---:|
| Source orders (HPOS-authoritative) | 8,209 |
| Date range | 2024-06-06 → 2025-11-10 |
| Total value | £474,050.49 |
| **Approved for import** (after D-1/D-2/D-4/D-5) | **8,053 / £464,097.26** |
| Held on open decisions | 154 / £9,910.23 |
| Excluded (abandoned checkout-drafts) | 2 / £43.00 |

**Owner decisions recorded 2026-08-24:**
- D-1: migrate all statuses except `wc-checkout-draft`
- D-2: guest orders — email only, no customer association
- D-4/D-5: absent products/variants — `productId: null`, retain title/sku/price

**Live schema introspection (API 2026-07) resolved three prior unknowns:**
- `inventoryBehaviour: BYPASS` is the exact required enum ("do not claim inventory")
- `RefundInput.processedAt` exists — refunds can be back-dated
- `OrderCreateLineItemInput.productId` is documented nullable — makes the
  262 missing-product orders importable without fabricating a product

**Still open:**
- **D-3** — 126 orders / £8,449.97: email-only (like guests) vs. hold until
  ADR-014 Gate 5 releases the 247 deferred customers. **No Shopify access
  needed to answer this.**
- **Gate O-1** — `read_orders`/`write_orders` not granted. Blocks all Phase 11
  live verification and execution. Independent of Phase 10's scope state.

**Standing safety constraints for any future Phase 11 work:** no executor
exists; `inventoryBehaviour: BYPASS`, `sendReceipt: false`,
`sendFulfillmentReceipt: false`, refund `notify: false` must be set explicitly
on every call — a pre-flight guard should refuse to run if any is missing.
Shopify orders cannot be deleted, so this is less reversible than Phase 9/10.

## Artifacts referenced above

- `migration/schema/phase10_migration_contract.json` — frozen Phase 10 contract + Gate 7 provenance
- `docs/PHASE11_DECISION_REGISTER.md` — D-1…D-11 with evidence and owner decisions
- `docs/PHASE11_EXECUTION_ARCHITECTURE.md` — gate definitions O-1…O-8, execution sequence
- `docs/PHASE11_TRANSFORMATION_CONTRACT.md` — field-by-field order mapping
- `reports/phase11_approved_scope.{json,csv}` — per-order disposition under current decisions
- `PHASE10_IMPORT_PROCEDURE.md` — corrected this session

## Pending actions as of this snapshot

1. Push `72f10d7` to `origin/phase10/gate7-executor-freeze`
2. Open PR to `main` using drafted body (session scratchpad) — **not ready to
   merge**, D-3 and ADR-014 still open
3. Answer D-3 (no Shopify access needed)
4. Resolve ADR-014 (business/legal)
5. Grant `read_orders`/`write_orders` to unblock Gate O-1
